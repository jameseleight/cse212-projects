 1.  long     is a type for long integer size is 64 bit rather than 32 bit.  The GeoJSON json file has values that are long rather than int.
the array bbox shown on the summary format page provided in the assignment is NOT in the json string.
some of the parameters are in a different order in the json string than in the summary description.  in metadata the order is different in the json string than in the summary description.  You can put the url for the json in your browser and it will pull up the json string.  The order in the json is generated, url, title, status, api, count.  Also, there is a item in features at the end called "title" which is not in the summary description.
It looks like the name of the attribute needs to match the text in the json string.  With the option of caseInsensitive the case looks to not matter, but you have to match the characters.  This is confusing to me since the lesson has a value iss_position with the underscore and the class attribute is IssPosition which doesn't have the underscore.  Another confusion from the lesson is that the code for the json deserializer does not include the option.  It looks like the default is case insensitive so perhaps more a confusion that the starter code in the SetsAndMaps.cs file uses the option (even though apparently it is the default.
If an attribute is set to a type in the class and the value in the json is null, then the was an exception thrown.  It looks like you can use ? to avoid the exception being thrown.  for example int? or string? etc. will run without an exception if the value is not an int but a null.  There are several parameters in the json with null even though the summary description provides a specific type.
The array [] items shown in the summary format do not have a class.  instead they are declared as a type of say double for the values in the summary format of longitude, latitude, and depth.